[project]
name = "astrbot-plugin-github-subscriber"
version = "0.1.2"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.9",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
]
